[package]
name = "ethabi_tests"
version = "0.1.0"
authors = ["debris <marek.kotewicz@gmail.com>"]

[dependencies]
ethabi = { path = "../ethabi" }
ethabi_derive = { path = "../derive" }
ethabi_contract = { path = "../contract" }
rustc-hex = "1.0"
